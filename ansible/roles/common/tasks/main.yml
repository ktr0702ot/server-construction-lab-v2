---
- name: パッケージリストを更新
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 3600

- name: 基本パッケージをインストール
  ansible.builtin.apt:
    name:
      - vim
      - curl
      - wget
      - git
    state: present